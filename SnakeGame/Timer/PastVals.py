import Fruit

class Follow:
    def __init__(self, x, y):
        self.x
        self.y
    
    coord = (x, y) 
    a = [coord]
    if(Fruit.collide == True):
        snakeLength +=1
    
    def update(tempC, snakeLength):
        a.insert(0, tempC)
        if(snakeLength <= (len(a)):
            a.remove(len(a)-1)
            
    
